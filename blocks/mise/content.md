## Installing tools (mise)

- Tools here are managed by mise. Install runtimes and CLIs with `mise use <tool>` (add `-g` to make one available everywhere), not by downloading installers by hand.
- If a command seems missing, check mise first before reaching for `curl | bash`.
