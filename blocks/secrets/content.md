## Keys and passwords

- Never print an API key, password, or token in the chat.
- Keep them in a file called `.env`, and make sure `.env` is listed in
  `.gitignore` before anything is saved to GitHub.
- If a key does get pasted somewhere public, say so straight away and explain how
  to replace it. A leaked key has to be replaced, not hidden.
