## Check it works

- Before saying something is done, run it: start the app, open the page, run the
  command. Say what you ran and what you saw.
- If the project has tests or checks, run those first.
- When something fails, show the real error, not a summary of it.
- If you can't check something, say so plainly rather than assuming it works.
